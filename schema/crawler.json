{
  "title": "THREDDS Crawler",
  "description": "THREDDS catalog crawler",
  "type": "object",
  "properties": {
    "maxtasks": {
      "type": "integer",
      "title": "Number of download tasks to run concurrently",
      "default": 10
    },
    "timeout": {
      "type": "integer",
      "title": "Maximum number of seconds to perform crawl",
      "default": 600
    },
  },
  "additionalProperties": false
}
